// main script for take the sums of square roots
function sumsOfSquareRoots(a, b, c) {
  let result = Math.ceil(Math.sqrt(a) + Math.sqrt(b) + Math.sqrt(c));
  return result;
}

// console.log(sumsOfSquareRoots(4, 4, 4));

function isItPythagoreanTriple(a, b, c) {}

console.log(Math.max.call());
